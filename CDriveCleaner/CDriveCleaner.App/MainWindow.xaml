<Window x:Class="CDriveCleaner.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="C: Drive Cleaner" Height="600" Width="900">
  <DockPanel>
    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="10" HorizontalAlignment="Right" Spacing="8">
      <Button Content="Analyze" Width="100" Command="{Binding AnalyzeCommand}" />
      <Button Content="Clean" Width="100" Command="{Binding CleanCommand}" />
    </StackPanel>

    <Grid Margin="10">
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <DataGrid Grid.Row="0"
                ItemsSource="{Binding Items}"
                AutoGenerateColumns="False"
                SelectionMode="Extended"
                CanUserAddRows="False"
                IsReadOnly="True">
        <DataGrid.Columns>
          <DataGridCheckBoxColumn Header="Select" Binding="{Binding Selected, Mode=TwoWay}" Width="60"/>
          <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
          <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
          <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="3*"/>
          <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="120"/>
        </DataGrid.Columns>
      </DataGrid>

      <StatusBar Grid.Row="1">
        <StatusBarItem>
          <TextBlock Text="{Binding Summary}" />
        </StatusBarItem>
      </StatusBar>
    </Grid>
  </DockPanel>
</Window>